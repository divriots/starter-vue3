import Layout from './theme/src/Layout.vue';

export default {
  Layout,
  // enhanceApp({ app }) {
  //   app.config.compilerOptions.isCustomElement = (tag) => {
  //     return tag.startsWith('dockit-');
  //   };
  // },
};
